#pragma once

#include "RoadRoute.h"
#include "RoadPoint.h"

#include <memory>

namespace trm
{
	/*class TrainMovablePart
	{
	public:
		TrainMovablePart(const RoadRoutePtr & r, const Heading h);

		virtual void Move() override;

	private:
		RoadPoint roadPoint_;
	};

	typedef std::unique_ptr<TrainMovablePart> TrainMovablePartUPtr;*/

} // namespace trm

		